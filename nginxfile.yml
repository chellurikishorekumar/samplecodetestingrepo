- hosts: all
  become: True # Use sudo/root for tasks
  tasks:
    - name: Install packages
      yum:
        name: "httpd"
        state: "present"
    - name: Start httpd server
      service:
        name: httpd
        state: started
        enabled: True
    - name: Deploy static website
      copy:
        src: /var/lib/jenkins/workspace/JenkinswithAnsible/files/index.html
        dest: /var/www/html/index.html
        remote_src: yes

